app:
  title: HealthPulse AI
  description: "Personal health companion — dietician, gym trainer, physician monitor, motivational coach"
  version: "0.1.0"
  cors_origins:
    - "*"

api:
  prefix: /api/v1
  port: 8080
  host: "0.0.0.0"

llm:
  provider: anthropic #"openai" #or "anthropic"
  openai:
    chat_model: gpt-4o-mini
    vision_model: gpt-4o-mini
  anthropic:
    chat_model: claude-haiku-4-5-20251001
    # vision_model: claude-sonnet-4-6
    vision_model: claude-haiku-4-5-20251001
    # orchestrator_model: claude-sonnet-4-5-20250929
    orchestrator_model: claude-haiku-4-5-20251001
    planner_model: claude-haiku-4-5-20251001
    synthesizer_model: claude-haiku-4-5-20251001
  max_tokens: 1024
  temperature: 0.7

planning:
  enabled: true
  always_plan: false           # only route compound / low-confidence queries to the planner
  fast_path_confidence_threshold: 0.85  # >= this -> fast path, < this -> planner
  max_steps: 6
  max_parallel: 3
  step_timeout_seconds: 45
  total_timeout_seconds: 90

storage:
  chat_media_bucket: my-health-chat-media
  presigned_url_ttl_seconds: 900
  session_list_page_size: 50
